import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Annuity Advisor Intelligence | SEC Variable Insurance Product Analysis Platform",
  description: "AI-powered platform for financial advisors to analyze variable annuities and RILAs using official SEC VIP data. Product comparisons, fee analysis, rider research, and compliance tools.",
  keywords: [
    "variable annuities",
    "RILA",
    "registered index-linked annuities",
    "SEC VIP data",
    "annuity analysis",
    "financial advisor tools",
    "insurance product research",
    "annuity fees",
    "GMWB",
    "GMIB",
    "death benefit riders",
    "SEC Forms N-3",
    "SEC Forms N-4",
    "SEC Forms N-6",
    "annuity comparison",
    "variable insurance products"
  ].join(", "),
  authors: [{ name: "Annuity Advisor Intelligence" }],
  creator: "Annuity Advisor Intelligence",
  publisher: "Annuity Advisor Intelligence",
  category: "Financial Services",

  // OpenGraph metadata for social sharing
  openGraph: {
    type: "website",
    locale: "en_US",
    url: "https://your-domain.com",
    title: "Annuity Advisor Intelligence | SEC Variable Insurance Analysis",
    description: "AI-powered research assistant for financial advisors. Analyze variable annuities and RILAs using official SEC VIP data.",
    siteName: "Annuity Advisor Intelligence",
    images: [
      {
        url: "/og-image.jpg", // You'll need to create this
        width: 1200,
        height: 630,
        alt: "Annuity Advisor Intelligence Platform",
      },
    ],
  },

  // Twitter Card metadata
  twitter: {
    card: "summary_large_image",
    title: "Annuity Advisor Intelligence",
    description: "AI Research Assistant for Variable Insurance Products",
    images: ["/twitter-image.jpg"], // You'll need to create this
  },

  // Robots metadata
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: true,
      "max-video-preview": -1,
      "max-image-preview": "large",
      "max-snippet": -1,
    },
  },

  // Additional metadata
  applicationName: "Annuity Advisor Intelligence",
  referrer: "origin-when-cross-origin",
  formatDetection: {
    email: false,
    address: false,
    telephone: false,
  },

  // Verification codes (add when you have them)
  // verification: {
  //   google: "your-google-verification-code",
  //   yandex: "your-yandex-verification-code",
  // },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <head>
        {/* Favicon */}
        <link rel="icon" href="/favicon.ico" sizes="any" />
        <link rel="icon" href="/icon.svg" type="image/svg+xml" />
        <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
        <link rel="manifest" href="/manifest.json" />

        {/* Theme color for mobile browsers */}
        <meta name="theme-color" content="#2563eb" />

        {/* Additional SEO tags */}
        <meta name="format-detection" content="telephone=no" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen flex flex-col bg-gray-50`}
      >
        <Header />
        <main className="flex-1">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}